export const metadata = {
  description:
    "BizOS is a business operating system built for Nigerian SMEs. It handles your contacts, invoices, tax records, payroll, and cash flow in one place.",
};

export default function BizOSLayout({ children }) {
  return children;
}
